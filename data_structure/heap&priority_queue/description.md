## 二叉堆

期望实现一种数据结构：寻找最大/小值：`O(1)`，插入：`O(log n)`，删除最大/小值：`O(log n)` 、

二叉堆：大根堆/小根堆：每一个节点的左右子节点都比他小/大，因此根节点是最大/小的节点，查找`O(1)`，，，一个二叉堆是一个完全二叉树，叶子节点只能从左往右，不允许中间出现空缺，，，，，对于（自底向上）建堆操作，从`currentsize/2`开始往前每个节点进行下滤操作，但是下滤的高度递增，`O(n)`

删除操作：删除根节点，将`currentsize`位置的节点移到根，然后进行下滤操作

**优先队列**：利用堆建立，元素按照优先级依次得到访问
